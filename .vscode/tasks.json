{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compila Rubrica",
            "type": "process",
            "command": "mvn",
            "args": ["-q", "compile"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
